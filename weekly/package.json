{
  "name": "talex-weekly",
  "private": true,
  "type": "module",
  "scripts": {
    "weekly": "node scripts/weekly.mjs",
    "prepare-data": "node scripts/weekly.mjs --prepare-only",
    "check": "node scripts/check.mjs",
    "check:all": "node scripts/check.mjs --all",
    "dev": "node scripts/weekly.mjs --prepare-only && npx --yes hyperframes@0.8.58 preview",
    "render:all": "node scripts/weekly.mjs --format all"
  }
}
